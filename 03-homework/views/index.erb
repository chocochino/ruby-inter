<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="/style.css">
    <title>Food list</title>
  </head>
  <body>
    <h1>Foods</h1>

    <button class="create-button">
      <a href="/create" class="plain-link">New Food</a>
    </button>

    <table>
      <thead>
        <tr>
          <th>Name</th>
          <th>Price</th>
          <th colspan="3">Action</th>
        </tr>
      </thead>    
      <tbody>
        <% items.each do |item| %>
          <tr class="food-list">
            <td><%= item.name %></td>
            <td><%= item.price %></td>
            <td><a href="/<%= item.id %>" class="plain-link">Show</a></td>
            <td><a href="/<%= item.id %>/edit" class="plain-link">Edit</a></td>
            <td>
              <form action="/<%= item.id %>" method="POST" style="padding: 0;">
                <input type="hidden" name="_method" value="DELETE">
                <input type="submit" value="Delete">
              </form>
            </td>
          </tr>
        <% end %>
      </tbody>
    </table>
  </body>
</html>
